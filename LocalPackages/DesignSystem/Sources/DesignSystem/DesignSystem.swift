// DesignSystem — MM2 토큰, 컴포넌트, SVG/이미지 리소스.
// SwiftUI만 의존. Domain import 금지.
//
// 토큰: Color+MM.swift / Spacing+MM.swift
// 좌표 매핑: MatchaOriginCoords.swift (ADR-302 v1.1 designer-lead Q1 합의)
//
// Phase 3 후속 (designer-lead):
// - Asset Catalog (Pretendard 폰트, MM2 컬러, MatchaPin SVG)
// - Components/MatchaPinView, MMButton, MMCard 등 components.md 기준
// - MMTypography (Pretendard / Noto Serif KR / IBM Plex Mono)

import SwiftUI

public enum DesignSystem {
    public static let version: String = "0.2.0"
}
